{
  "version": 2,
  "framework": "vite",
  "buildCommand": "pnpm install --frozen-lockfile && pnpm prisma generate && pnpm run build:client",
  "outputDirectory": "dist/spa",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
